var express=require("express"),fs=require("fs"),path=require("path"),bodyParser=require("body-parser"),routes=require("./routes"),user=require("./routes/user"),upload=require("./routes/upload");var app=express();app.set("port",3000);app.use(bodyParser({uploadDir:"/uploadtest"}));app.use(function(d,c,a,b){console.error(d.stack);a.send(500,"服务器端错误")});app.get("/",routes.index);app.post("/login",user.login);app.post("/workUpload",upload.upload);app.get("/workUpload",upload.upload);app.listen(app.get("port"));